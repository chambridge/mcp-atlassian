apiVersion: v1
kind: Service
metadata:
  name: mcp-atlassian
  namespace: {{NAMESPACE}}
  labels:
    app: mcp-atlassian
    component: service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: {{MCP_PORT}}
    protocol: TCP
  selector:
    app: mcp-atlassian
